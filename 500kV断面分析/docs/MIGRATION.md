# 迁移记录

原项目目录：`~/Documents/chao liu fen xi`

当前维护目录：`500kV断面分析/`

本次迁移包括：

- 清洗、断面计算、网页生成和兼容GUI源码。
- 500kV拓扑、接线图、已确认坐标及位置候选数据。
- 节点位置校准网页和辅助脚本。
- 原项目测试与设计文档。
- 原始业务样本及历史生成结果，归档于被Git忽略的 `local_data/`。

所有运行入口和默认路径均已改为仓库相对路径，不再依赖原项目的绝对路径。原项目在迁移验证完成后已移入 macOS 废纸篓；当前项目目录是唯一维护入口。
